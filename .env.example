# Frontend environment variables
# If set, the app will post to `${VITE_API_BASE_URL}/api/...`
# If not set, it will call `/api/...` on the same origin (works with Vite dev proxy)
# VITE_API_BASE_URL=http://localhost:5000
